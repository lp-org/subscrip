{
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "start": "dotenv turbo run start",
    "dev": "dotenv turbo run dev",
    "watch": " turbo run watch",
    "lint": "turbo run lint",
    "generate": "npm run generate --workspace=apps/loaders",
    "drop": "npm run drop --workspace=apps/loaders",
    "push": "npm run push --workspace=apps/loaders",
    "seed": "dotenv turbo run seed",
    "studio": "dotenv turbo run studio",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "eslint": "7.32.0",
    "eslint-config-custom": "*",
    "prettier": "^2.5.1",
    "turbo": "latest"
  },
  "packageManager": "npm@8.19.4",
  "dependencies": {
    "clsx": "^2.0.0",
    "dotenv-cli": "^7.2.1"
  }
}
