import React from "react";
import { Metadata } from "next/types";
import StorePage from "../../../components/view/StorePage";

const Store = () => {
  return <StorePage />;
};
export default Store;

export const metadata: Metadata = {
  title: "Store",
};
