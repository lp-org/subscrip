import React from "react";
import PricingForm from "../../../../../../components/view/PricingForm";

const page = () => {
  return <PricingForm />;
};

export default page;

export const metadata = {
  title: "Create Pricing",
};
